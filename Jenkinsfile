pipeline {
  
  agent any
  
  stages {
  
    stage('Git clone') {
      steps {
        
        git branch:'main', url://
        echo "cloning the code from git repository"
      }
    
    
    }
  
  
  
  }
}
